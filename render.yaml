databases:
  - name: aeroplanes
  ipAllowList: []
  databaseName: aeroplanes
  user: aeroplanes_user

services:
  - type: web
    name: aeroplne
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: aeroplanes
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false